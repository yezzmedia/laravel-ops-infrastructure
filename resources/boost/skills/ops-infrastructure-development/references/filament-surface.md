# Filament Surface

The infrastructure Filament plugin currently owns one operator page:

- `OpsInfrastructurePage`

Keep that page aligned with the single declared ops module `infrastructure.overview`.
